{
  "name": "chaoxing-sign-web-docs",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "wrangler": "^4.93.0"
  }
}
